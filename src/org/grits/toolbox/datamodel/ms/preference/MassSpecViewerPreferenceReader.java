package org.grits.toolbox.datamodel.ms.preference;

import org.grits.toolbox.datamodel.ms.tablemodel.dmtranslate.DMPeak;
import org.grits.toolbox.datamodel.ms.tablemodel.dmtranslate.DMPrecursorPeak;
import org.grits.toolbox.datamodel.ms.tablemodel.dmtranslate.DMScan;
import org.grits.toolbox.display.control.table.datamodel.GRITSColumnHeader;
import org.grits.toolbox.display.control.table.preference.TableViewerPreferenceReader;

public class MassSpecViewerPreferenceReader extends TableViewerPreferenceReader {

//	@Override
//	public SimianColumnHeader getColumnHeader(String _sKey) {
//		if ( _sKey.equals(DMScan.scan_scanNo.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_scanNo.getLabel(), DMScan.scan_scanNo.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_pseudoScanNo.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_pseudoScanNo.getLabel(), DMScan.scan_pseudoScanNo.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_parentScan.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_parentScan.getLabel(), DMScan.scan_parentScan.name() );
//		}		
//		if ( _sKey.equals(DMScan.scan_retentionTime.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_retentionTime.getLabel(), DMScan.scan_retentionTime.name());
//		}
//		if ( _sKey.equals(DMScan.scan_scanStart.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_scanStart.getLabel(), DMScan.scan_scanStart.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_scanEnd.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_scanEnd.getLabel(), DMScan.scan_scanEnd.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_msLevel.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_msLevel.getLabel(), DMScan.scan_msLevel.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_polarity.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_polarity.getLabel(), DMScan.scan_polarity.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_activationMethode.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_activationMethode.getLabel(), DMScan.scan_activationMethode.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_numsubscans.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_numsubscans.getLabel(), DMScan.scan_numsubscans.name() );
//		}
//		if ( _sKey.equals(DMScan.scan_numannotatedpeaks.name() ) ) {
//			return new SimianColumnHeader( DMScan.scan_numannotatedpeaks.getLabel(), DMScan.scan_numannotatedpeaks.name() );
//		}
//		if ( _sKey.equals(DMPeak.peak_id.name() ) ) {
//			return new SimianColumnHeader( DMPeak.peak_id.getLabel(), DMPeak.peak_id.name() );
//		}
//		if ( _sKey.equals(DMPeak.peak_mz.name() ) ) {
//			return new SimianColumnHeader( DMPeak.peak_mz.getLabel(), DMPeak.peak_mz.name() );
//		}
//		if ( _sKey.equals(DMPeak.peak_intensity.name() ) ) {
//			return new SimianColumnHeader( DMPeak.peak_intensity.getLabel(), DMPeak.peak_intensity.name() );
//		}
//		if ( _sKey.equals(DMPeak.peak_relative_intensity.name() ) ) {
//			return new SimianColumnHeader( DMPeak.peak_relative_intensity.getLabel(), DMPeak.peak_relative_intensity.name() );
//		}
//		if ( _sKey.equals(DMPeak.peak_charge.name() ) ) {
//			return new SimianColumnHeader( DMPeak.peak_charge.getLabel(), DMPeak.peak_charge.name() );
//		}
//		if ( _sKey.equals(DMPrecursorPeak.precursor_peak_mz.name() ) ) {
//			return new SimianColumnHeader( DMPrecursorPeak.precursor_peak_mz.getLabel(), DMPrecursorPeak.precursor_peak_mz.name() );
//		}
//		if ( _sKey.equals(DMPrecursorPeak.precursor_peak_intensity.name() ) ) {
//			return new SimianColumnHeader( DMPrecursorPeak.precursor_peak_intensity.getLabel(), DMPrecursorPeak.precursor_peak_intensity.name() ); 
//		}
//		if ( _sKey.equals(DMPrecursorPeak.precursor_peak_relative_intensity.name() ) ) {
//			return new SimianColumnHeader( DMPrecursorPeak.precursor_peak_relative_intensity.getLabel(), DMPrecursorPeak.precursor_peak_relative_intensity.name() ); 
//		}
//		if ( _sKey.equals(DMPrecursorPeak.precursor_peak_charge.name() ) ) {
//			return new SimianColumnHeader( DMPrecursorPeak.precursor_peak_charge.getLabel(), DMPrecursorPeak.precursor_peak_charge.name() );
//		}
////		System.out.println("MassSpecViewerPreferenceReader.getColumnHeader unrecognized: " + _sKey);		
//		return null;
//	}
	
}
